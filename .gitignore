# Godot 4+ specific ignores
.godot/
/android/

# Visual Studio's per-user cache and solution state. Rewritten on every
# build, so tracking it means a dirty tree after simply opening the project.
.vs/
